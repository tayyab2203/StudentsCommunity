'use client';

// This file should NEVER be imported directly
// It's only loaded via dynamic import in BackgroundAnimation.js
// All Three.js imports are done inside the component to prevent SSR evaluation

export default function BackgroundAnimationClient() {
  // This will be replaced with actual implementation via dynamic import
  return null;
}
